{
  "name": "hexfactory",
  "version": "0.38.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:wasm": "wasm-pack build ./factory-wasm --target web --out-dir pkg --out-name factory_wasm",
    "build:wasm:bench": "wasm-pack build ./factory-wasm --target web --out-dir pkg-bench --out-name factory_wasm_bench -- --features bench",
    "build": "npm run build:wasm && npm run typecheck && vite build",
    "lint": "eslint .",
    "format": "prettier --check . && cargo fmt --manifest-path factory-wasm/Cargo.toml -- --check",
    "format:write": "prettier --write . && cargo fmt --manifest-path factory-wasm/Cargo.toml",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "test:run": "vitest run",
    "test:rust": "cargo test --manifest-path factory-wasm/Cargo.toml",
    "audit": "npm audit --audit-level=high",
    "bench": "cargo run --release --manifest-path factory-wasm/Cargo.toml --bin capacity --",
    "survey": "cargo run --release --manifest-path factory-wasm/Cargo.toml --bin survey --",
    "balance": "cargo run --release --manifest-path factory-wasm/Cargo.toml --bin balance --",
    "bench:browser": "npm run build:wasm:bench && vite",
    "agent:map": "node scripts/agent-map.mjs",
    "agent:map:check": "node scripts/agent-map.mjs --check",
    "quality": "npm run audit && npm run agent:map:check && npm run format && npm run lint && npm run typecheck && npm run test:run && npm run test:rust && npm run build"
  },
  "dependencies": {
    "@hexlife/embed": "1.15.0",
    "three": "0.185.1"
  },
  "devDependencies": {
    "@eslint/js": "9.30.0",
    "@types/node": "24.13.3",
    "@types/three": "0.185.4",
    "eslint": "9.30.0",
    "globals": "16.2.0",
    "prettier": "3.6.2",
    "typescript": "5.8.3",
    "typescript-eslint": "8.35.1",
    "vite": "6.4.3",
    "vitest": "4.1.8"
  }
}
